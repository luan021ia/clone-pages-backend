[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "bash -c 'chmod +x scripts/init-db.sh 2>/dev/null || true; bash scripts/init-db.sh 2>/dev/null || true; node dist/main.js'"
